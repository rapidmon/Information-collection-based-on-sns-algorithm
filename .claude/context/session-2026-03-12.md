# 세션 요약 — 2026-03-12

## 프로젝트 상태
- **브랜치**: `main`
- **최신 커밋**: `0072225` — 🎨 GitHub Pages Dark SF 테마 적용 및 카테고리 필터 수정
- **push 상태**: origin/main 대비 1커밋 ahead (push 아직 안 함)

### 수정된 파일
| 파일 | 변경 내용 |
|------|----------|
| `docs/css/style.css` | Dark SF 글래스모피즘 테마 전체 적용 (CSS 변수, 카드, 배지, 버튼, 인풋 등) |
| `docs/index.html` | 대시보드 페이지 다크 테마 마크업 동기화 |
| `docs/briefings.html` | 브리핑 아카이브 페이지 다크 테마 마크업 동기화 |
| `docs/posts.html` | 게시물 탐색 페이지 다크 테마 마크업 동기화 |
| `docs/js/app.js` | 플립 카드 렌더링, 배지 시스템, 스켈레톤 카드 등 다크 테마 대응 |
| `docs/js/firestore-api.js` | 카테고리 필터 클라이언트 사이드 전환 + 기본 카테고리 폴백 추가 |

---

## 완료된 작업

1. **GitHub Pages(docs/) Dark SF 테마 동기화**
   - `src/presentation/web/templates/`(Jinja2)의 다크 테마를 `docs/`(정적 HTML)에 수동 포팅
   - CSS 변수, 글래스모피즘 카드, 네온 glow, 스캔라인 애니메이션 등 전체 디자인 시스템 적용

2. **카테고리 드롭다운 빈 문제 수정**
   - Firestore `categories` 컬렉션이 비어있을 때 드롭다운에 옵션이 없던 문제
   - `DEFAULT_CATEGORIES` 배열(AI, Semiconductor, Cloud, BigTech, Startup, Regulation, Coding) 폴백 추가

3. **카테고리 필터 에러 수정**
   - `is_relevant == true` + `category_names array-contains` + `orderBy` 복합 쿼리에 Firestore 인덱스 없어서 에러
   - 카테고리 필터를 Firestore 쿼리에서 제거 → 클라이언트 사이드 필터링으로 전환
   - 카테고리 필터 시 `limitCount * 3`배 데이터를 가져와서 충분한 결과 확보

4. **로컬 프리뷰 환경 구성**
   - `npx serve` (포트 3000)로 docs/ 정적 파일 서빙
   - Playwright MCP로 브라우저 테스트 (검색, 카테고리 필터 동작 확인)

---

## 진행 중인 작업
- 없음 (모든 요청 작업 완료)

---

## 핵심 결정사항

1. **두 프론트엔드 구조 유지**
   - `src/presentation/web/templates/` — Jinja2 (로컬 Python 서버용)
   - `docs/` — 정적 HTML + Firebase (GitHub Pages용)
   - 변경 시 양쪽 수동 동기화 필요

2. **카테고리 필터: 클라이언트 사이드 방식 채택**
   - Firestore 복합 인덱스 생성 대신 클라이언트에서 필터링
   - 트레이드오프: 네트워크 데이터 약간 증가 vs 인덱스 관리 불필요
   - `limitCount * 3`배로 가져오므로 대부분 충분한 결과 확보

---

## 다음 단계

1. **`git push`** — 커밋은 완료했지만 push는 사용자가 원할 때 실행
2. **GitHub Pages 배포 확인** — push 후 https://rapidmon.github.io/Information-collection-based-on-sns-algorithm/ 에서 다크 테마 적용 확인
3. **소스 필터 테스트** — 카테고리 필터는 검증 완료, 소스(twitter/threads 등) 필터도 테스트 권장
4. **Firestore 복합 인덱스 생성 (선택)** — 성능 최적화 시 Firebase 콘솔에서 인덱스 추가 가능

---

## 주의사항

- `docs/js/firebase-config.js`에 Firebase 설정이 하드코딩되어 있음 (프로젝트: `sns-algorithm`)
- `npx serve`로 로컬 테스트 시 포트 3000 사용 (백그라운드 프로세스 남아있을 수 있음)
- `.playwright-mcp/` 폴더가 untracked로 생성됨 — `.gitignore`에 추가 권장
- `.claude/settings.local.json`도 수정됨 (Playwright MCP 도구 허용 추가) — 커밋하지 않음
